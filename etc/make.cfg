# Directories
PKGDIR='/var/otto/pkg'
HIUSR='/usr/local'
LOUSR='/usr'
BIN='/bin'
ETC='/etc'
HIUSRBIN="${HIUSR}${BIN}"
LOUSRBIN="${LOUSR}${BIN}"
HIETC="${HIUSR}${ETC}"
LOETC="${ETC}"
HISRCDIR="${HIUSR}/src"
LOSRCDIR='/var/otto/src'
CONTENTS='/var/otto/installed/contents'

# Options
MIRROR='https://luiznet.tk/otto/packages'
AUTOCLEAN='NO' # YES for cleaning automatically compilation files (source-code and sort of).
EDITOR='ed' # Ed is the standard text editor.
DROP_SHELL=''
SYNC_TYPE='bittorrent' # If torrents are too slow, you can use http/ftp instead.
MKPREFIX="${USRDIR}" # Don't change this.

# CPUFLAGS
CFLAGS=''
CXXFLAGS=${CFLAGS}
KCFLAGS=${CFLAGS}
